Hier die vier *.sql-Dumps ablegen.

`Haupttabelle___hs_du.sql` um Folgendes erweitern (zu Beginn des Skripts):

``` SQL
CREATE DATABASE IF NOT EXISTS `hsdb`
CHARACTER SET = 'utf8'
COLLATE = 'utf8_unicode_ci';
USE `hsdb`;
```